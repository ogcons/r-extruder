management:
  info:
    java:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
server:
  shutdown: graceful
spring:
  lifecycle:
    timeout-per-shutdown-phase: 1m